services:
  ruby:
    prepare:
      - echo "prepare service 1"
      - echo "prepare service 2"
    from: 'ruby:2.2.3'

defaults: &server
  prepare:
    - git --version
    - ls -alh websocket-demo
    - env
    - echo "prepare victim"
  service: ruby
  folder: /app
  env:
    - RACK_ENV=production

staging:
  <<: *server
  port: 3001
  cmd: 'ruby -run -e httpd . -p 3001'

tests:
  <<: *server
  cmd: 'echo "..............................."'
